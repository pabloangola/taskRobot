import { OnChanges, SimpleChanges } from '@angular/core';
export declare class HandlePropChanges implements OnChanges {
    handlers: Object;
    ngOnChanges(changes: SimpleChanges): void;
    executePropHandlers(props?: Object): void;
}
