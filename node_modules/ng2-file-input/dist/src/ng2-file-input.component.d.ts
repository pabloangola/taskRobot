import { EventEmitter, OnInit } from '@angular/core';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
import { Ng2FileInputOptions } from './ng2-file-input.interface';
export declare class Ng2FileInputComponent implements OnInit {
    private sanitizer;
    private defaultOptions;
    private alreadyEmitted;
    private options;
    fileIsOver: boolean;
    invalidFile: boolean;
    dropText: string;
    browseText: string;
    removeText: string;
    invalidFileText: string;
    invalidFileTimeout: number;
    multiple: boolean;
    removable: boolean;
    showPreviews: boolean;
    extensions: string[];
    output: EventEmitter<{}>;
    currentFiles: File[];
    constructor(sanitizer: DomSanitizer, defaultOptions: Ng2FileInputOptions);
    ngOnInit(): void;
    fileOver(fileIsOver: boolean): void;
    onFileDrop(file: File): void;
    fileSelected(event: any): void;
    removeFile(file: File): void;
    getObjectUrl(file: File): SafeResourceUrl;
    private handleFile(file);
    private isValidFile(file);
}
